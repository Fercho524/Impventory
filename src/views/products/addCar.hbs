<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">

                    <h3>
                        {{producto.nombre}}
                    </h3>

                    <p>
                        $ {{producto.precioVenta}} c/u
                    </p>

                    <form action="/products/addCar/{{producto.id}}" method="POST">

                        <div class="form-group">
                            <input type="number" value="{{producto.existencia}}" name="existencia" class="form-control"
                                placeholder="Unidades Disponibles">
                        </div>

                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Añadir al carrito
                            </button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>